#include<stdio.h>
int main()
{
    // int x,y;
    // x=10;
    // y=x++;    // post increment
    // printf("%d %d",x,y);
    // return 0;

    int x,y;
    x=10;
    y=++x;    // pre increment
    printf("%d %d",x,y);
    return 0;

    return 0;
}